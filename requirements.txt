# Core LangGraph + Anthropic (existing)
langgraph>=1.1.0
langchain-anthropic>=1.4.0
langchain-core>=1.0.0
anthropic>=0.40.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
structlog>=24.0.0

# Web API (Phase 1)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9

# Multi-provider LLM (Phase 2) — install only what you need
langchain-openai>=0.1.1
langchain-community>=0.0.36
dashscope>=1.14.0
