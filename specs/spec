# 金融多Agent系统｜技术选型+模块分工+落地规范（对标同花顺问财+东财妙想）
## 核心结论（前置）
1. 架构范式：**主控调度Agent + 领域Sub-Agent**（双巨头通用架构）
2. 编排核心：**LangGraph** 做流程/状态/循环/校验，**摒弃纯LangChain**
3. 防幻觉核心：**强工具锁+数据强制校验+输出风控**（金融刚需）
4. 工程核心：**全链路追踪+分层熔断+规则配置化+合规拦截**
5. 数据核心：**金融结构化API+专属知识库+RAG检索**，杜绝LLM杜撰

---

## 一、全局架构总览（对标大厂）
### 分层架构
```
前端交互层
  ↓
接入层（网关+鉴权+限流+输入前置风控）
  ↓
调度核心层（路由Agent + LangGraph编排引擎 + 全局状态中心）
  ├─ 领域子Agent集群（诊股/选股/客服/闲聊）
  ├─ 统一工具网关（金融数据/RAG/检索/风控）
  ├─ 全局校验层（语义校验+合规拦截+结果校验）
  ↓
基础能力层（LLM集群+金融数据中台+知识库+存储）
```

### 核心运行逻辑
1. 输入预处理 → 敏感词过滤 → 会话状态绑定
2. 路由Agent：**意图识别+场景判定+上下文纠偏**
3. 路由分发至对应Sub-Agent，多意图自动拆分并行
4. 子Agent执行：思考→工具调用→数据校验→结果整合
5. 后置风控+合规润色+强制风险提示
6. 输出+会话状态持久化+全链路日志记录

---

## 二、核心技术选型（生产级）
### 1. Agent编排核心
- 编排框架：**LangGraph**（唯一选择）
  - 解决：多分支、循环校验、状态共享、人工干预、断点续跑
  - 替代：LangChain 纯Chain线性架构（无法适配金融复杂流程）
- 基础组件：LangChain（工具封装、Prompt、LLM适配器、RAG工具）
- 执行模式：**异步Graph执行**，支持并行工具调用、多子Agent并发

### 2. 模型分层选型（金融专用）
| 模型类型 | 用途 | 选型建议 |
|---------|------|----------|
| 通用大模型 | 闲聊、普通客服、基础对话 | 轻量化大模型，控成本 |
| 金融专用大模型 | 诊股、选股、行情分析、研报解读 | 金融微调模型（降低行业幻觉） |
| 小模型/轻量模型 | 意图识别、场景分类、敏感检测、风控 | 小语义模型，低延迟 |

### 3. 数据与存储
- 会话状态：Redis（实时状态、场景标签、上下文缓存）
- 对话持久化：MongoDB（长文本对话历史）
- 结构化金融数据：第三方金融API（行情/财报/指标/舆情）
- 知识库&检索：
  - 结构化FAQ：MySQL
  - 研报、行业资料、规则文档：**向量库（Milvus）+ ES**
- 缓存体系：多级缓存（接口缓存、热点数据缓存、问答缓存）

### 4. 工具层架构
- 统一工具网关：所有金融工具**中心化注册+权限+限流+熔断**
- 工具分类：
  1. 金融数据工具：行情、财报、估值、行业、板块、指标
  2. 检索工具：知识库RAG、FAQ检索、研报检索
  3. 风控工具：敏感检测、违规识别、投资建议拦截
  4. 通用工具：时间、基础问答

---

## 三、模块拆分与职责分工（精准落地）
### 1. 路由调度Agent（核心中枢，对标问财AIME引擎）
**核心职责**
- 多轮上下文解析、用户意图识别、场景判定
- 场景切换控制、混合问句拆分、多Agent调度
- 全局流程控制、超时控制、异常降级
**核心能力**
- 三级识别：关键词强匹配 → 轻量模型意图 → 上下文修正
- 场景锁定：单轮锁定，强指令才切换
- 冲突处理：多意图问题拆分、多子Agent并行汇总

### 2. 四大领域Sub-Agent（完全解耦）
#### ① 诊股Agent
- 业务：单股分析、基本面、K线、行业对标、财报解读
- 强制约束：**禁止涨跌预测、禁止利好利空定论**
- 工具依赖：个股行情、财报、估值、研报、行业数据

#### ② 选股Agent
- 业务：板块分析、指标筛选、行业趋势、量化指标解读
- 强制约束：**禁止个股推荐、禁止投资策略推荐**
- 工具依赖：行业榜单、指标库、板块数据、行情排行

#### ③ 客服Agent
- 业务：产品咨询、费用、规则、账号、业务问题
- 核心模式：**强RAG+固定知识库**，答案100%溯源
- 工具依赖：FAQ知识库、业务接口、工单系统

#### ④ 闲聊Agent
- 业务：泛闲聊、情绪互动、非金融问答
- 约束：弱风控、轻量化、避免冗余

### 3. 全局状态中心（LangGraph核心）
**统一State字段设计**
```
session_id、user_id、current_scene、scene_history
dialog_history、user_context、tool_call_log
risk_check_result、error_flag、temp_params(股票代码/关键词)
```
- 全节点共享读写，支持状态快照、断点、回溯
- 场景标签全局持久化，保证多轮连贯性

### 4. 双轨风控体系
- 前置风控：输入敏感词、违禁词、违规关键词拦截
- 后置风控：语义识别、违规话术过滤、投资建议拦截、强制风险提示
- 规则库：配置化管理，动态更新，无需改代码

---

## 四、核心难点+解决方案（金融专属）
### 1. 难点1：多场景混淆、混合问句
- 方案：**意图拆分引擎** + 多Agent并行调用 + 结果融合
- 落地：LangGraph多分支并行节点，自动合并回答

### 2. 难点2：LLM金融数据幻觉
- 方案：**数据强制锁**
  - 所有数值、行情、财报、指标类问题 → 必须调用工具
  - 无数据 → 直接拒绝回答，禁止杜撰
  - 数据结果二次校验（数值范围、合理性校验）

### 3. 难点3：金融合规高压
- 方案：
  - 子Agent专属Prompt约束，固化禁忌话术
  - 统一合规模板，所有专业回答附加免责声明
  - 违规案例沉淀，持续迭代风控模型

### 4. 难点4：多轮上下文串扰
- 方案：
  - 场景隔离上下文，不同Agent独立上下文窗口
  - 全局场景标签约束，抑制跨场景冗余信息

### 5. 难点5：高并发+稳定性
- 方案：
  - 子Agent资源隔离，熔断降级
  - 金融API限流、缓存、熔断机制
  - LLM分层降级，大模型故障切换小模型兜底

---

## 五、工程化落地规范（含可观测性）
### 1. 全链路可观测体系（对标大厂）
#### ① 全链路追踪
- 工具：LangSmith / 自研Trace
- 追踪维度：
  - 会话ID、用户ID、场景路由结果
  - 每个Agent执行耗时、LLM调用、工具调用记录
  - 输入输出、Token消耗、异常日志

#### ② 核心监控指标
**业务指标**
- 场景识别准确率、子Agent调用占比、问答命中率
- RAG召回率、知识库匹配率

**性能指标**
- 单轮响应耗时、LLM耗时、工具调用耗时
- QPS、超时率、错误率、接口失败率

**风控指标**
- 敏感拦截数、违规话术拦截数、风险提示覆盖率

#### ③ 日志与告警
- 全结构化日志：Agent名称、场景、工具、风控、异常
- 实时告警：LLM异常、API熔断、高频违规、超时暴涨

### 2. 工程稳定性设计
1. **多级降级**
   - 子Agent异常 → 通用兜底回答
   - 金融数据故障 → 关闭数据分析，仅基础回复
2. **配置化架构**
   - 场景规则、Prompt、风控词、阈值、工具开关 → 动态配置
3. **异步+隔离**
   - 全异步执行，子Agent资源隔离，防止雪崩

### 3. 迭代策略
1. 灰度上线：先闲聊+客服（低风险）→ 诊股→选股
2. 离线迭代：对话样本沉淀、意图模型迭代、风控规则更新
3. 闭环优化：日志分析→问题汇总→Prompt/规则/模型迭代

---

## 六、落地实施步骤（最简落地路线）
1. **第一步**：搭建LangGraph核心框架+全局State+路由Agent
2. **第二步**：接入基础LLM+统一工具网关+基础风控
3. **第三步**：落地低风险Agent：闲聊→客服
4. **第四步**：搭建金融数据工具+RAG知识库，落地诊股Agent
5. **第五步**：完善选股Agent+全链路风控强化
6. **第六步**：部署可观测、监控、告警、熔断，生产上线
